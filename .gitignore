node_modules/
dist/
.vite/
*.log
.DS_Store
*.tsbuildinfo

# 本地环境变量（含内网后端地址）
.env.local

# Playwright 产物（trace / screenshot / report）
test-results/
playwright-report/
blob-report/

# REPO-HYGIENE-01：审计截图全量集**外置**（CI artifact / 发布仓），
# 仓库只保留 samples/ 抽样（≤1MiB 门禁守住）。策略见 docs/evidence-policy.md。
docs/evidence/**/full/
